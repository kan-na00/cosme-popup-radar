name: 毎日イベント収集

on:
  schedule:
    # 毎日 07:00 JST (= 前日 22:00 UTC) に実行
    - cron: "0 22 * * *"
  workflow_dispatch: {} # 手動実行も可能

permissions:
  contents: write

concurrency:
  group: daily-collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリを取得
        uses: actions/checkout@v4

      - name: Node.js をセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 依存関係をインストール
        run: npm ci

      - name: イベントを収集
        run: npm run collect

      - name: 変更があればコミット＆プッシュ
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/events.json
          if git diff --staged --quiet; then
            echo "更新なし: コミットをスキップします"
          else
            git commit -m "chore: 毎日のイベントデータ更新"
            git push
          fi

      - name: Vercel 本番へデプロイ
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: team_y6GFIYvCNz2e5xdmhJFskBjg
          VERCEL_PROJECT_ID: prj_Cde864ihOAcW4vIwhyZZU0gZ5R2d
        run: |
          npx --yes vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
          npx --yes vercel build --prod --token="$VERCEL_TOKEN"
          npx --yes vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN"
